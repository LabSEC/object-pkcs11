#!/usr/bin/make -f
# debian.rules
# Be carefull, this is a Makefile, so the indents are tabs and not spaces!
# -*- makefile -*-

export DESTROOT=$(CURDIR)/debian/object-pkcs11

%:
	dh $@

override_dh_auto_install:
	dh_auto_install
	install -p -D -m 0755 libobjectpkcs11.so $(DESTROOT)/usr/lib
